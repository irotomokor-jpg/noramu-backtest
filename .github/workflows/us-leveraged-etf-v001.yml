name: US Leveraged ETF v0.01 MA200 Research

on:
  workflow_dispatch:
  push:
    branches:
      - "agent/us-leveraged-etf-v001-ma200"
    paths:
      - "us_leveraged_etf_v001_ma200.py"
      - "requirements.txt"
      - ".github/workflows/us-leveraged-etf-v001.yml"

permissions:
  contents: write

concurrency:
  group: us-leveraged-etf-v001-ma200
  cancel-in-progress: false

jobs:
  backtest:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Compile and self-test
        run: |
          python -m py_compile us_leveraged_etf_v001_ma200.py
          python us_leveraged_etf_v001_ma200.py --self-test

      - name: Run predeclared 24-variant matrix
        run: |
          rm -rf us_leveraged_etf_v001_output
          python us_leveraged_etf_v001_ma200.py \
            --cache-dir us_leveraged_etf_v001_cache \
            --outdir us_leveraged_etf_v001_output

      - name: Verify and print result
        run: |
          test -f us_leveraged_etf_v001_output/RUN_VALIDATION.txt
          grep -q '^PASS' us_leveraged_etf_v001_output/RUN_VALIDATION.txt
          grep -q 'NO_ORDERS' us_leveraged_etf_v001_output/RUN_VALIDATION.txt
          cat us_leveraged_etf_v001_output/scorecard.json
          cat us_leveraged_etf_v001_output/robustness_decisions.csv
          cat us_leveraged_etf_v001_output/listed_buy_hold_baselines.csv

      - name: Upload research artifact
        continue-on-error: true
        uses: actions/upload-artifact@v4
        with:
          name: us-leveraged-etf-v001-${{ github.run_number }}
          path: us_leveraged_etf_v001_output/
          if-no-files-found: warn
          retention-days: 90

      - name: Persist research result to branch
        if: success()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add us_leveraged_etf_v001_output
          if git diff --cached --quiet; then exit 0; fi
          git commit -m "us-etf: persist v0.01 MA200 results [skip ci]"
          BRANCH="${GITHUB_REF_NAME}"
          for attempt in 1 2 3 4; do
            git fetch origin "$BRANCH"
            if ! git rebase "origin/$BRANCH"; then git rebase --abort || true; exit 1; fi
            if git push origin "HEAD:$BRANCH"; then exit 0; fi
            sleep $((attempt * 2))
          done
          exit 1
