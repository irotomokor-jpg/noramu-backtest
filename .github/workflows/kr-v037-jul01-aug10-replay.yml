name: KR v0.37 Jul 01-Aug 10 Replay Audit
on:
  workflow_dispatch:
  push:
    branches: [agent/kr-v037-jul01-aug10-replay-audit]
    paths:
      - 'kr_v037_jul01_aug10_replay_audit.py'
      - '.github/workflows/kr-v037-jul01-aug10-replay.yml'
permissions:
  contents: write
jobs:
  replay:
    runs-on: ubuntu-latest
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/kr-v037-jul01-aug10-replay-audit
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: pip install -U pandas numpy yfinance pyarrow requests lxml html5lib beautifulsoup4
      - name: Compile
        run: python -m py_compile kr_v037_jul01_aug10_replay_audit.py kr_v036_aug03_10_replay_audit.py kr_v036_replay_hotfix.py
      - name: Run expanded replay audit
        run: python kr_v037_jul01_aug10_replay_audit.py --outdir kr_v037_replay_output
      - name: Verify outputs
        run: |
          test -s kr_v037_replay_output/scorecard.json
          test -f kr_v037_replay_output/replay_event_log.csv
          test -f kr_v037_replay_output/minute_execution_audit.csv
          test -f kr_v037_replay_output/replay_daily_summary.csv
          test -f kr_v037_replay_output/replay_monthly_summary.csv
          test -f kr_v037_replay_output/intraday_fidelity_by_day.csv
          cat kr_v037_replay_output/scorecard.json
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: kr-v037-jul01-aug10-replay
          path: kr_v037_replay_output/
      - name: Persist result to branch
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -f kr_v037_replay_output
          git diff --cached --quiet || git commit -m 'kr: persist v0.37 Jul01-Aug10 replay result [skip ci]'
          git pull --rebase origin agent/kr-v037-jul01-aug10-replay-audit
          git push origin HEAD:agent/kr-v037-jul01-aug10-replay-audit
